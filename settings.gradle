rootProject.name = 'concurrencyStudy'

include 'mutex'
include 'semaphore'
include 'optimistic'
include 'pessimistic'

def includeProject(String name, String path){
    include(name)
    if(path != null){
        project(name).projectDir = new File(path)
    }
}

